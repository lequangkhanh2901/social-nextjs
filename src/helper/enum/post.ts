export enum RegimePost {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE',
  ONLY_FRIEND = 'ONLY_FRIEND',
  CUSTOM_ONLY = 'CUSTOM_ONLY',
  CUSTOM_EXCLUDE = 'CUSTOM_EXCLUDE'
}

export enum RegimeCreatePost {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE',
  ONLY_FRIEND = 'ONLY_FRIEND',
  CUSTOM = 'CUSTOM'
}

export enum MediaType {
  IMAGE = 'IMAGE',
  VIDEO = 'VIDEO',
  PDF = 'PDF'
}

export enum PostType {
  NORMAL = 'NORMAL',
  MEDIA = 'MEDIA',
  STATUS = 'STATUS'
}
