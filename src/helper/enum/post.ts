export enum RegimePost {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE',
  ONLY_FRIEND = 'ONLY_FRIEND'
}

export enum MediaType {
  IMAGE = 'IMAGE',
  VIDEO = 'VIDEO'
}

export enum PostType {
  NORMAL = 'NORMAL',
  MEDIA = 'MEDIA',
  STATUS = 'STATUS'
}
